# System Architecture: Aura Studio

## 🖼️ Architecture Diagram Submission

*(REQUIRED) Where did you upload an architecture diagram in the image Gallery or File Upload?*

**Answer:** The high-resolution architecture diagram has been uploaded to the **Image Gallery / File Upload** section of the submission portal. 

Below is a detailed breakdown and a text-based visual representation of how Aura Studio connects the frontend, the Gemini API, and the Firebase backend.

---

## 🏗️ System Flow & Architecture

Aura Studio is built as a serverless, client-heavy application. The React frontend directly orchestrates calls to the Gemini API for multimodal generation and communicates with Firebase for authentication and data persistence.

### Visual Representation (Mermaid)

```mermaid
graph TD
    subgraph Frontend [Client-Side (React 19 + Vite)]
        UI[User Interface / React Components]
        AI_Service[AI Service Layer / @google/genai]
        FB_Service[Firebase Service Layer]
    end

    subgraph Google AI [Google Gemini API]
        Gemini_Text[Gemini 3 Flash Preview<br/>Text, Summaries & JSON Schema]
        Gemini_Image[Gemini 3.1 Flash Image Preview<br/>Visual Asset Generation]
        Gemini_Audio[Gemini 2.5 Flash TTS<br/>Audio/Voiceover Generation]
    end

    subgraph Firebase [Backend Infrastructure]
        Auth[Firebase Authentication<br/>Google Sign-In]
        Firestore[Cloud Firestore<br/>Campaigns, Media & Archives]
    end

    %% User Interactions
    User((User)) -->|Inputs Text & Config| UI
    
    %% AI Flow
    UI -->|Triggers Generation| AI_Service
    AI_Service -->|1. Analyze & Generate Text| Gemini_Text
    Gemini_Text -->|Returns JSON (Posts & Prompts)| AI_Service
    AI_Service -->|2. Generate Images (Parallel)| Gemini_Image
    Gemini_Image -->|Returns Base64 Images| AI_Service
    AI_Service -->|3. Generate Audio (Parallel)| Gemini_Audio
    Gemini_Audio -->|Returns Base64 Audio| AI_Service
    AI_Service -->|Returns Multimodal Campaign| UI

    %% Backend Flow
    UI -->|Login/Logout| FB_Service
    FB_Service <-->|OAuth Token| Auth
    
    UI -->|Save Campaign/Post| FB_Service
    FB_Service -->|Save Metadata & Base64 Media| Firestore
```

## 🧩 Component Breakdown

### 1. Frontend (React 19 + Vite + Tailwind CSS)
- **User Interface:** Handles user input, configuration (platform selection, tone, pro-mode toggles), and displays the interleaved multimodal output.
- **AI Service Layer (`aiService.ts`):** Manages the orchestration of Gemini API calls. It handles the sequential and parallel execution of text, image, and audio generation, ensuring rate limits are respected (e.g., batching TTS requests).
- **Firebase Service Layer (`firebase.ts`):** Manages the connection to Firebase, handling authentication state and Firestore CRUD operations.

### 2. Google AI (Gemini API via `@google/genai`)
The application uses the official `@google/genai` SDK directly from the client side to interact with three distinct models:
- **`gemini-3-flash-preview`**: Acts as the "brain". It analyzes the source text, generates a summary, and outputs a strictly typed JSON schema containing the platform-specific captions, hashtags, and highly detailed visual descriptions (prompts) for the image model.
- **`gemini-3.1-flash-image-preview`**: Takes the visual descriptions generated by the text model and produces contextual, high-quality images in Base64 format.
- **`gemini-2.5-flash-preview-tts`**: Takes the generated captions and produces natural-sounding voiceovers in raw PCM format, which the frontend converts to playable WAV files.

### 3. Backend (Firebase)
- **Firebase Authentication:** Secures the application using Google Sign-In, ensuring that users can only access and modify their own generated content.
- **Cloud Firestore:** Stores the campaign metadata, including the text captions, hashtags, summaries, and the Base64 strings for the media generated by Gemini. Security rules ensure data isolation between users.

